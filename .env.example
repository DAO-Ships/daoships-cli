# Optional plaintext source for explicit import or legacy migration only.
# chmod 600 this file, then:
# daoships --key-env-file .env wallet import captain DAOSHIPS_PRIVATE_KEY
# Remove plaintext sources after verifying an encrypted backup.
DAOSHIPS_PRIVATE_KEY=
# Signing uses the stored keystore. Supply --password-file, or export
# DAOSHIPS_KEYSTORE_PASSWORD_FILE in the shell; it is not loaded from .env.
